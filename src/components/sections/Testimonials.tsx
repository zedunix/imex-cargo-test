"use client";
import { useEffect,useRef,useState } from "react";
import { testimonials } from "../../content";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
gsap.registerPlugin(ScrollTrigger);
export default function Testimonials(){const root=useRef<HTMLElement>(null);const[active,setActive]=useState(0);useEffect(()=>{const reduce=matchMedia("(prefers-reduced-motion:reduce)").matches,mobile=matchMedia("(max-width:767px)").matches;const ctx=gsap.context(()=>{if(reduce||mobile)return;ScrollTrigger.create({trigger:root.current,start:"top top",end:`+=${testimonials.length*720}`,pin:true,scrub:true,onUpdate:self=>setActive(Math.min(testimonials.length-1,Math.floor(self.progress*testimonials.length)))})},root);return()=>ctx.revert()},[]);return <section className="testimonials section-pad" ref={root}><div className="testimonial-head"><p className="eyebrow">Client signals / Placeholder quotes</p><span>{String(active+1).padStart(2,"0")} / {String(testimonials.length).padStart(2,"0")}</span></div><div className="quote-mark">“</div><div className="quotes">{testimonials.map((t,i)=><blockquote className={active===i?"active":""} key={t.name}><p>{t.quote}</p><footer><strong>{t.name}</strong><span>{t.role}</span></footer></blockquote>)}</div><div className="quote-dots">{testimonials.map((_,i)=><button key={i} onClick={()=>setActive(i)} className={active===i?"active":""} aria-label={`Show testimonial ${i+1}`}/>)}</div></section>}
