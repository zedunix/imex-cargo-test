"use client";
import { FormEvent, useEffect, useRef, useState } from "react"; import gsap from "gsap"; import { ScrollTrigger } from "gsap/ScrollTrigger"; import { services } from "../../content"; gsap.registerPlugin(ScrollTrigger);
export default function CTAFooter() { const root = useRef<HTMLElement>(null); const [sent, setSent] = useState(false); useEffect(() => { const ctx = gsap.context(() => gsap.from(".cta-title", { scale: .86, opacity: 0, color: "#ff4d00", duration: .7, ease: "back.out(1.4)", scrollTrigger: { trigger: root.current, start: "top 65%" } }), root); return () => ctx.revert(); }, []); const submit = (e: FormEvent) => { e.preventDefault(); setSent(true); }; return <footer className="cta section-pad" ref={root}><p className="eyebrow">Start a shipment</p><h2 className="cta-title">LET’S MOVE<br/><span>FORWARD.</span></h2><form onSubmit={submit}><label>Name<input required placeholder="Your name" /></label><label>Company<input required placeholder="Company name" /></label><label>Phone<input type="tel" placeholder="+00 000 000 0000" /></label><label>Email<input required type="email" placeholder="you@company.com" /></label><label>Service<select defaultValue=""><option value="" disabled>Select service</option>{services.map(s => <option key={s.number}>{s.title}</option>)}</select></label><label className="wide">Message<textarea rows={3} placeholder="Tell us what needs to move" /></label><button className="submit" type="submit">{sent ? "REQUEST RECEIVED ✓" : "SEND REQUEST ↗"}</button></form><div className="footer-row"><div className="wordmark">NORTH<span>LINE</span></div><nav><a href="#">LinkedIn</a><a href="#">Instagram</a><a href="mailto:hello@example.com">hello@example.com</a></nav><span>© 2026 Placeholder Logistics Co.</span></div></footer>; }
