"use client";
import { useEffect, useRef } from "react"; import gsap from "gsap"; import { ScrollTrigger } from "gsap/ScrollTrigger"; import { countries } from "../../content"; gsap.registerPlugin(ScrollTrigger);
export default function GlobalReach() { const root = useRef<HTMLElement>(null); useEffect(() => { const ctx = gsap.context(() => gsap.from(".country", { y: 30, opacity: 0, stagger: .08, duration: .55, scrollTrigger: { trigger: root.current, start: "top 65%", toggleActions: "play none none reverse" } }), root); return () => ctx.revert(); }, []); return <section className="reach section-pad" ref={root}><p className="eyebrow">Global reach / Connected locally</p><h2>ONE NETWORK.<br/><span>EVERY DIRECTION.</span></h2><div className="country-grid">{countries.map((c, i) => <div className="country" key={c}><small>{String(i + 1).padStart(2, "0")}</small>{c}<b>↗</b></div>)}</div></section>; }
