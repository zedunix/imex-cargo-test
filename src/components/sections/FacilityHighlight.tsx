"use client";
import { useEffect, useRef } from "react"; import gsap from "gsap"; import { ScrollTrigger } from "gsap/ScrollTrigger"; import { facilityFeatures } from "../../content"; gsap.registerPlugin(ScrollTrigger);
export default function FacilityHighlight() { const root = useRef<HTMLElement>(null); useEffect(() => { const ctx = gsap.context(() => { gsap.utils.toArray<HTMLElement>(".facility-copy").forEach(block => ScrollTrigger.create({ trigger: block, start: "top 55%", end: "bottom 45%", onToggle: self => block.classList.toggle("active", self.isActive) })); gsap.to(".facility-ring", { rotate: 180, ease: "none", scrollTrigger: { trigger: root.current, start: "top bottom", end: "bottom top", scrub: true } }); }, root); return () => ctx.revert(); }, []); return <section className="facility section-pad" ref={root}><div className="facility-visual"><div className="facility-ring" /><div className="facility-label"><span>NX—01</span><strong>SMART<br/>FACILITY</strong><small>25.2048° N / 55.2708° E</small></div></div><div className="facility-text"><p className="eyebrow">Inside the operation</p>{facilityFeatures.map(f => <article className="facility-copy" key={f.number}><span>{f.number}</span><h3>{f.title}</h3><p>{f.copy}</p></article>)}</div></section>; }
